#include <iostream>
#include "timeDifferenceHistogramGenerator.hpp"

RegularHistogram getHistogram( int argc , char* argv[] ) {
  // get filenames of folders to read
  // bind lamnda for cutoff depending on input
  // create a histogram for each file
  // congolmerate the histograms
}


int main(int argc , char* argv[]) {
  RegularHistogram hist = getHistogram(argc , argv);
}
